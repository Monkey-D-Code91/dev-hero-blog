---
/**
 * Homepage IT — temporaneamente mostra il profilo di Marco Mariotti.
 * Sarà sostituita dalla landing del blog in Fase C2.
 */
import { getCollection, render } from "astro:content";
import AuthorLayout from "../layouts/AuthorLayout.astro";

// Carica il profilo IT di Marco dalla collection authors.
const allAuthors = await getCollection("authors");
const entry = allAuthors.find((a) => a.id === "it/marco-mariotti");
if (!entry) throw new Error("Author it/marco-mariotti not found in collection");

const { Content } = await render(entry);
const author = entry.data;
---

<AuthorLayout
  author={author}
  lang="it"
  itHref="/"
  enHref="/en/"
  title="Marco Mariotti — Software Engineer & Tech Lead"
  description={author.subline}
>
  <Content />
</AuthorLayout>
